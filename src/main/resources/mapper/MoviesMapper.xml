<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chen.cloudshadow.mapper.MoviesMapper">

    <!-- 开启二级缓存 -->
    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.chen.cloudshadow.pojo.Movies">
        <id column="movie_id" property="movieId" />
        <result column="title" property="title" />
        <result column="description" property="description" />
        <result column="duration" property="duration" />
        <result column="release_date" property="releaseDate" />
        <result column="genre" property="genre" />
        <result column="director" property="director" />
        <result column="cast" property="cast" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        movie_id, title, description, duration, release_date, genre, director, cast, created_at, updated_at
    </sql>

</mapper>
